package np.gov.digital.platformevidenceexchange.enums;

/** The three external authoritative sources named in Governance Tiers §7. */
public enum EvidenceAgency {
    /** District Administration Office — citizenship facts. */
    DAO,
    /** National ID Management Center — National ID facts. */
    NIDMC,
    ELECTION_COMMISSION
}
