package np.gov.digital.platformbenefits.enums;

/**
 * Scoped to the two categories citizen-registry's EligibilityService
 * actually evaluates today — SENIOR/SINGLE_WOMAN/FARMER are that
 * module's own unimplemented Phase 2 placeholders (IdCardType), same
 * scope boundary platform-idcard's DocumentType already follows.
 */
public enum BenefitType {
    DISABILITY,
    UNEMPLOYMENT
}
